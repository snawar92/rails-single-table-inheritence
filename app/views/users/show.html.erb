<p id="notice"><%= notice %></p>

<p>
  <strong>First name:</strong>
  <%= @user.first_name %>
</p>

<p>
  <strong>Last name:</strong>
  <%= @user.last_name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>


<h1>Emergency Contacts</h1>
<%= link_to 'New', new_user_emergency_path(@user) %>
<table class='table'>
  <thead>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Phone Number</th>
      <th>Birthday</th>
      <th>actions</th>
    </tr>
  </thead>
  <tbody>
    <% @user.emergencies.each do |contact| %>
      <tr>
        <td><%= contact.first_name %></td>
        <td><%= contact.last_name %></td>
        <td><%= contact.phone_number %></td>
        <td><%= contact.birthday %></td>
        <td><%= link_to 'delete', [@user, contact], method: :delete %> ... <%= link_to 'edit', edit_user_emergency_path(@user, contact) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<h1>Friends Contacts</h1>
<%= link_to 'New', new_user_friend_path(@user) %>
<table class='table'>
  <thead>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Phone Number</th>
      <th>Birthday</th>
      <th>actions</th>
    </tr>
  </thead>
  <tbody>
    <% @user.friends.each do |contact| %>
      <tr>
        <td><%= contact.first_name %></td>
        <td><%= contact.last_name %></td>
        <td><%= contact.phone_number %></td>
        <td><%= contact.birthday %></td>
        <td><%= link_to 'delete', [@user, contact], method: :delete %>  ... <%= link_to 'edit', edit_user_friend_path(@user, contact) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
